# AI编译规则 – 多智能体记忆池

## 输入源
- 目录：`_RawChats/`（人工筛选的聊天记录导出，Markdown格式）
- 每个文件聚焦一个话题，允许少量无关内容。

## 输出目标
1. **分级目录**  
   - 路径：`wiki/1_健康/`, `wiki/2_学习/`, `wiki/3_AI与技术/`, `wiki/4_哲学与心理/`, `wiki/5_职业与规划/`, `wiki/6_生活与工具/`
   - 每个概念生成或更新一个`.md`文件。

2. **时间轴**  
   - 路径：`timeline/YYYY-MM.md` 或 `timeline/待办池.md`
   - 有明确日期（如“2026-05-22”或“5月22日”）→ 写入对应月份文件。  
   - 模糊时间（“下周”“近期”）→ 写入 `timeline/待办池.md`，注明来源。

## 笔记模板（分级目录）

```markdown
# 概念名称

## 核心结论
（从对话中提炼的一致看法）

## 演变/过程
（旧观点 → 新观点，若无则省略）

## 待解决问题
（对话中提出的疑问或未决之事）

## 来源
- 对话文件：`_RawChats/xxx.md`
- 原始片段：> 关键原句
冲突处理
若新结论与已有笔记矛盾：不覆盖原笔记，而是在“演变/过程”中追加记录：

markdown
- **旧观点**（来源：文件A）：...
- **新观点**（来源：当前文件）：...
若为补充信息：直接追加到“核心结论”或新增小节。

忽略内容
单句闲聊（“嗯”“好的”“哈哈”）

明显跑题的无关内容

任务提取
动作关键词：学习、完成、购买、练习、写、复习、整理

只有明确日期才自动写入时间轴月份文件。

无明确日期 → 写入 timeline/待办池.md，格式：- [ ] 任务描述 （期望时间：模糊描述） ← 来源：文件名

执行指令（人类发给AI时的模板）
“请按照 AGENTS.md 规则编译 _RawChats/[文件名].md。输出到分级目录和时间轴。遇到矛盾时追加到演变过程，末尾标注来源。忽略无关闲聊。”

text

### 3. `docs/ARCHITECTURE.md`（中文）

```markdown
# 系统架构

## 数据流
用户对话（如“承安”）
→ 人工按话题筛选
→ _RawChats/*.md
→ 编译器（牧之）读取 AGENTS.md
→ 调用大模型（本地/云端）
→ 生成/更新：

wiki/（分级目录）

timeline/（时间轴任务）

text

## 模块说明

| 模块 | 职责 | 技术 |
|------|------|------|
| 原始存储 | 保存完整聊天记录 | `_RawChats/`（可用Git LFS） |
| 编译器（牧之） | 执行编译规则 | 大模型（DeepSeek、MiMo等） + 手动触发 |
| 记忆池 | 结构化知识 + 时间轴 | Markdown文件 + Obsidian |
| 版本控制 | 追踪变更、回滚 | Git |

## 创新点

- **编译时记忆**（非运行时RAG）：确定性、可解释。
- **人在回路冲突解决**：永不静默覆盖。
- **时间轴与分级目录双向链接**：将行动与知识关联。

## 未来集成

- Hermes Agent只读查询
- 多智能体权限：每个专家Agent拥有私有工作记忆 + 共享记忆池只读权
